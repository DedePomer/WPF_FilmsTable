<UserControl x:Class="WPF_SQL.View.Pages.MenuPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_SQL.View.Pages"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Command="{Binding NavigateToFilmsTablePageCommand}" Height="80" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1"  Background="White">
            <Border Background="White" Padding="4">
                <fa:ImageAwesome Icon="Database" Foreground="Black"/>
            </Border>
        </Button>

    </Grid>
</UserControl>
